# Project configuration
general:
  projectName: 'RiskManagement'
  productiveBranch: 'main'

# Stage configuration
stages:
  productionDeployment:
    cfTargets:
    - org: 'b02efa9atrial'
      space: 'dev'
      appName: 'RiskManagement'
      apiEndpoint: 'https://api.cf.us10.hana.ondemand.com'
      credentialsId: 'cf-credintial'